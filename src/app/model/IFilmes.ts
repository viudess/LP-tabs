/* eslint-disable eol-last */
export interface IFilmes {
  nome: string;
  lancamento: string;
  duracao: string;
  classificacao: number;
  cartaz: string;
  generos: string[];
  pagina?: string;
  favorito: boolean;
}
